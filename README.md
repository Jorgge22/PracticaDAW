# PracticaDAW

## COMANDOS **FUNDAMENTALES**

Primero: `git clone` y la url

Dspués **SI** o **SI** hacer `git pull` para descargar los cambios que ha subido el otro.

Cada vez que se cambia algo: `git add .` y luego `git commit -m "mensaje"`

Y despues `git push -u origin main`
